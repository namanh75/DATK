<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>HustCARE</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
    <link rel="stylesheet" href="/css/book.css">
    <script src="https://code.jquery.com/jquery-3.5.0.js"></script>
</head>

<body>
    <section class="book" id="book">
        <h1 class="heading">
            <span>b</span>
            <span>o</span>
            <span>o</span>
            <span>k</span>
            <span class="space"></span>
            <span>n</span>
            <span>o</span>
            <span>w</span>
        </h1>
        <div class="row">
            <div class="image">
                <img src="/image/{{doctor.stt}}.jpg" alt="">
                <h2>{{doctor.name}}</h3>
                    <p>{{doctor.description}}</p>
            </div>
            <form action="/doctor/schedule?d={{doctor.name}}" method="post">
                
                <div class="inputBox">
                    <h3>Họ và Tên</h3>
                    <input type="text" name="username" disabled="true" value="{{userdata.name}}" required>
                </div>
                <div class="inputBox">
                    <h3>Khám cho ai</h3>
                    <input type="text" name="for" placeholder="Bạn đăng ký khám cho bản thân hay cho người khác?" required >
                </div>
                <div class="inputBox">
                    <h3>Số điện thoại</h3>
                    <input type="text" placeholder="Để lại số điện thoại để nhân viên có thể liên hệ lại với bạn" name="phone" required>
                </div>
                <div class="inputBox">
                    <h3>Email</h3>
                    <input type="text" placeholder="Email" name="email" required>
                </div>
                <div class="inputBox">
                    <h3>Năm sinh (của người muốn khám)</h3>
                    <input type="date" name="birthday" placeholder="Bạn sinh năm bao nhiêu" required>
                </div>

                <div class="inputBox">
                    <h3>Địa chỉ</h3>
                    <input type="text" placeholder="Địa chỉ nhà bạn ở đâu?" name="address" required>
                </div>
                <div class="inputBox">
                    <h3>Ngày khám</h3>
                    <input type="date" placeholder="Lựa chọn ngày khám" name="timeday" required>
                </div>
                <div class="inputBox">
                    <h3>Giờ khám (Chọn 8h - 9h - 10h - 14h - 15h - 16h - 17h - 18h - 19h)</h3>
                    <input type="time" placeholder="Lựa chọn giờ khám" name="timehours" required>
                </div>
                <div class="inputBox">
                    <h3>Lý do khám</h3>
                    <textarea class="rea" cols="10" rows="5" name="description" placeholder="Bạn đang cảm thấy như thế nào?" required></textarea>
                </div>
                <input type="submit" class="btn" value="Đặt lịch">
            </form>
        </div>
    </section>

    <script>
        document.querySelector('form .btn').onclick = function(e) {
            e.preventDefault()
        }
    </script>
    <script src="/js/schedule.js"></script>
</body>
</html>